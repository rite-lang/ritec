struct A
    a: usize

impl A
    fn new() -> A
        A
          a: 42

    fn get_a(&mut self) -> usize
        self.a

fn main(argc: usize, argv: **u8) -> usize
    let a = A::new()
    let x = A::get_a(&a)
    x
