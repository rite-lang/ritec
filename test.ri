struct Vec<'a>
    ptr: *mut 'a
    len: usize
    cap: usize

impl Vec<'a>
    pub fn new() -> Self
        Self
            ptr: null
            len: 0
            cap: 0 

fn main(argc: usize, argv: **u8) -> usize
    0
