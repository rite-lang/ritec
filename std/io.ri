//! IO operations.

import std:string

/// Print a string to the standard output.
#[language("io:print")]
pub fn print(a: str) -> void
  panic

/// Print a string to the standard output followed by a newline.
pub fn println(a: str) -> void
  let newline = string:from_bytes([10])
  print(string:concat(a, newline))
