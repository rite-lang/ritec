import std:fs
import std:debug
import std:array
import std:string
import std:result:Result
import std:io

fn main(args: [str])
  string:slice("hello", 0, 1) |> debug:print
  1 + 12 as u8 |> debug:print
