import std:list
import std:result

fn main()
  [1, 2, 3, 4, 5]
  |> list:map(|y| y + 1)

fn is_even(y, x) -> bool
  *y = *y + 1
  (x % 2) == 0

fn not_five(x)
  match x == 5
  | true -> result:Err("Five")
  | false -> result:Ok(x)
